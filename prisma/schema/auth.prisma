model LoginAttempt {
  id        String   @id @default(cuid())
  ipAddress String
  email     String?
  count     Int      @default(1)
  expiresAt DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ipAddress])
}
